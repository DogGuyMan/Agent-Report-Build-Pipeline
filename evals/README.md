# evals/ — 실행기가 잰 값을 쌓는 곳

**왜 있는가.** 세 mode 실행기(`codegraph/run_mode*.py`)가 `--json` 으로 단계별 시간·토큰·비용을
낸다. 그 값을 버리면 "개선했다" 를 숫자로 말할 수 없다. 여기 쌓아 두고 **전후를 대조한다.**

**이 폴더는 판정하지 않는다.** 원본 측정값과 그 조건만 적는다. 좋아졌는지 나빠졌는지는
표를 보는 사람이 정한다.

## 기록하는 법

```bash
.venv/bin/python codegraph/run_mode1.py <저장소> --model opus \
  --json evals/runs/$(date +%F)-mode1-<대상>-<조건>.json
```

파일 이름 규칙 — `<날짜>-<mode>-<대상>-<조건>.json`.
조건에는 무엇이 달랐는지를 적는다(`cold` · `warmup` · `haiku` 따위).
**같은 조건이 아니면 대조하지 않는다.**

## 지금까지 잰 것

| 날짜 | mode | 대상 | 조건 | 벽시계 | 합계 토큰 | 캐시 비율 | 비용 | 턴 |
|---|---|---|---|---|---|---|---|---|
| 2026-08-30 | 1 | QtVisionEdit (cpp 77파일) | 냉시동 · opus | 27분 08초 | 17,925,770 | 97.3% | $15.4991 | 84 |
| 2026-08-30 | 1.5 | 이 저장소의 warmup 계획서 | 배선 연습 · haiku | 2분 28초 | 270,134 | — | $0.2252 | 6 |
| 2026-08-30 | 2 | 연습 프로젝트 | 배선 연습 · haiku | 2분 29초 | 347,012 | 96.1% | $0.1841 | 10 |

**세 번 재서 세 번 같은 모양이 나왔다 — LLM 한 칸이 전체 시간의 99% 를 쓴다.**
Mode 1 은 99.1%, Mode 2 는 99.2%. 기계 단계 합은 각각 15.2초 · 1.1초다.
그러므로 이 파이프라인을 빠르게 하거나 싸게 하는 길은 **에이전트가 읽는 양을 줄이는 것** 하나뿐이다.

## 다음에 잴 것

`docs/superpowers/plans/2026-08-30-warmup-mode1-wiring.md` 의 Task 9.
warmup 을 끼운 뒤 같은 대상을 다시 재서 위 첫 줄과 대조한다.
**대조는 `total`(캐시 포함)로 한다** — 캐시가 97.3% 라 `input` 만 보면 절감이 안 보인다.

## 배선 연습 값을 실사용으로 읽지 마라

haiku 두 줄은 **배선이 도는지**만 본 것이다. 🔵 그 산출물은 품질 검사를 통과하지 못했다 —
Mode 1.5 는 정답을 24문항 모두 첫 번째 자리에 놓았고(`validate_questions` 가 잡는다),
Mode 2 는 `<Section>` 을 자식 없이 내서 `tsc` 가 잡았다. 실사용에는 상위 모형이 필요해 보인다.
💭 표본이 각각 1건이라 그 이상은 말할 수 없다.
