# HANDOFF ⑪ — Mode 2 용어 관계도 물리 조정 (mode-2-spec-report 용 프롬프트)

> 🔴 **완료됨 (2026-08-29 17:45).** `mode-2-spec-report` DONE(테스트 91, 브라우저 없는 배치 시뮬로 퍼짐 1589→319px 관측). 사용자 육안: 이번엔 덩어리가 **너무 겹침** → ⑫(경계 충돌 + 슬라이더)로 이어짐. ⑫ 와 함께 `6ef58b3` 로 커밋. 진입점은 `RESUME-2026-08-29-mode-1-5-orchestrator.md` 다.

**사용자 지시 (2026-08-29 16:45)** — `src/runtime/term-graph.ts`: (1) 노드가 서로 밀어내는 힘이 너무 강하다. 독립 그래프(덩어리)끼리 너무 밀어내 가독성이 떨어진다 —
독립 덩어리 사이의 척력을 약화하라. (2) 배치 공간을 캔버스 가로·세로 **2배** 상자로 제한하고 밖으로 못 나가게 하라.

**설계** — `forceManyBody(-420)` 를 **연결 성분을 아는 쌍 전수 척력**으로 교체(같은 덩어리 `-300`, 다른 덩어리 `-60`, `260px` 밖 0). `forceCenter` → 노드별 약한 중력 `forceX/forceY(0.05)`.
tick 과 드래그에서 좌표를 `[-w/2, 3w/2] × [-h/2, 3h/2]` 로 자름. 조정 상수 5개는 파일 머리에. 순수 계산은 `src/runtime/graph-math.ts` 로 분리해 테스트.
물리의 좋고 나쁨은 **사용자 육안**으로 판정 — 상수만 바꿔 재빌드한다.

프롬프트 본문은 오케스트레이터 세션의 Agent 호출과 같다 — 7블록. 소유: `src/runtime/term-graph.ts` · `src/runtime/graph-math.ts`(신규) · `test/graph-math.test.mjs`(신규).
