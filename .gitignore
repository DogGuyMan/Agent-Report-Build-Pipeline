node_modules/

# 테스트용 esbuild 중간 산출물 (scripts/lib.mjs)
.tmp/
.tmp-report.mjs

# 빌드 산출물 — 소스에서 재생성한다 (인계 문서 §B-3 "git에는 소스만 커밋")
out/
**/specs/*/out/

# Phase 1 검증용 임시 이식본 (scripts/patch-legacy.mjs)
*.b1.html

.DS_Store

# Track C 파이프라인 (C-12) — networkx/pydot/lxml
.venv/

# Track C 다이어그램 산출물 — codegraph 에서 재생성한다
out/diagrams/
.tmp-report-tsconfig.json

# 파이썬 바이트코드 캐시 - 소스 실행 시 자동 재생성된다
__pycache__/

# 로컬 유지보수 도구 - 기계마다 다른 경로가 규칙에 박혀 있어 공유하지 않는다
scripts/scrub_local_paths.py
